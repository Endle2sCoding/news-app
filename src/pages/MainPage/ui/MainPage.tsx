import { AppContainer } from "@/shared/helpers/ui/AppContainer/AppContainer";
import s from "./MainPage.module.scss";
export function MainPage() {
  return (
    <main className={s.mainPage}>
      <AppContainer>MainPage</AppContainer>
    </main>
  );
}
